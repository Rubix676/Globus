(() => {
  // Disabled: this file previously overrode course-card media and conflicted with the main styles.
  // Course cards, history image, math image and modal behavior are now left to the existing CSS/JS files.
})();
